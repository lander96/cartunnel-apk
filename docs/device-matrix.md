# Device verification matrix

No iCAR device information or validation logs have been supplied. **未做 iCAR 真机验证**.

Do not mark emulator results as iCAR results. Record only model, firmware, API, ABI, network type and a de-identified timestamp when hardware is available.
