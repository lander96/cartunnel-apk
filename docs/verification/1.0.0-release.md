# CarTunnel 1.0.0 验收记录

2026-09-17，基于 `de82f6e`，分支 `codex/vmess-only-1.1.0`。

## 修改范围

- 横竖屏节点列表与导入、配置/设置行的纵向间距统一为现有 `control_gap`（10dp）；保留其余布局、控件尺寸及视觉资源。
- 首次使用通过共享 AppCompat 对话框展示 MIT 全文及中文免责声明。接受成功后保存安装级状态；拒绝退出；旋转、重启和覆盖安装保留正确状态。后台恢复在未同意时不启动 VPN。
- 自主开发代码采用根目录 MIT LICENSE；第三方组件仍受各自许可证约束。MIT 原文来源：https://opensource.org/license/mit 。APK 包含相同原文。
- README 精简为简介、已支持功能、安装使用、协议及免责，包含用户指定的 ADB 授权命令。
- release 版本名 1.0.0，versionCode 12，包名 com.cartunnel.client.debug。按用户明确选择沿用原签名证书，以便覆盖安装；使用 release 构建、R8 与资源压缩，debuggable=false。

## 验证

- 67 个 JVM 单元测试通过，无失败/错误/跳过。
- 17 个 Android 15 arm64 模拟器测试通过，无失败/跳过。新增同意/拒绝/重建/重启回归，以及横竖屏三个相邻间距的实际像素断言。
- Lint、assembleDebugAndroidTest、assemblePreviewRelease、assembleRelease 全部通过。
- Android UI 静态审计与 Premium strict audit：0 findings。DESIGN.md lint：0 errors、18 warnings（既有镜像 token 报告）。
- 最终 APK 验证 v2 签名、版本、包名、不可调试、权限白名单、arm64-only、native Deflate 与体积通过。使用 `--allow-debug` 明确允许用户选择的原签名证书。
- 最终 release APK 在 emulator-5580 覆盖安装成功，手工检查首次协议提示和同意进入主页，节点与下方按钮间距截图通过。
- 测试初稿在点击退出后过早断言，修正为等待实际 Activity DESTROYED；断言未削弱。截图等待系统旋转动画后重新采集。

## 产物

- `artifacts/1.0.0/CarTunnel.apk`
- `artifacts/1.0.0/CarTunnel.apk.sha256`
- 大小：12,334,621 bytes
- SHA-256：`8ac496d187f5ef56895bf1f1099cb0ed2a2f477b319e93a6c0f79468d37a493c`
- 签名证书 SHA-256：`f988ebba9b31078954ce2a778f86cb7b57cdd220d66030527cb5d2d5814b0fee`

构建、审计、测试日志与截图位于 [1.0.0-artifacts](1.0.0-artifacts)。

## 验证边界

本次未进行 iCAR 真机回归或真实 VPN 协议端到端重测；既有连接、节点管理逻辑未改动。锁定的 Xray native 16 KiB 对齐警告仍为既有事项。本次交付本地 release APK，未上传服务器或创建远端 Release。
