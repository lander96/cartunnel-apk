# Locked HEV build

Source: hev-socks5-tunnel `2.17.1`, commit `9a06bc6e7989da54e3d32ff701ef7a7ce4995d3a`, including its locked submodules. Run `scripts/build-hev-core.sh` with the pinned NDK. Only `arm64-v8a` is permitted.
