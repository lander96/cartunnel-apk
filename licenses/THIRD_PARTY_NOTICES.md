# Third-party notices

The complete, verbatim license texts distributed with the source inputs are in this directory: `LICENSES/`. AndroidLibXrayLite (LGPL-3.0-or-later) and Xray-core (MPL-2.0) build inputs are vendored under `core-xray/upstream`; HEV (MIT), its task system (MIT), lwIP (BSD-3-Clause), and libyaml (MIT) are vendored under `native/hev/upstream`. Exact commits and artifact hashes are in `config/versions.lock` and `native/hev/jni/artifacts.sha256`; the build-time generated component inventory is written to `build/component-inventory.json`.
